package controller;

/**
 * Initializes the Model (board) and the View (displays the board).
 */
public interface Controller {

  /**
   * Supplies the view with the model and initiates the game.
   */
  void play();

}
